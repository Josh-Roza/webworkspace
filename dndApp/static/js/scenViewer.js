function summon(monsterName){
    if (pass){
        pass
        //if the monster is already there add another health mod
    }
    else{
        newDiv = document.createElement("div");
        newDiv.innerHTML += pass;
    }
}
    
